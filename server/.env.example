# ============================================================
# Modified: See CHANGELOG.md for complete modification history
# Last Updated: 2025-10-28
# Modified By: jimyungkoh<aqaqeqeq0511@gmail.com>
# ============================================================

# 서버 설정
# 서버가 실행될 포트 (기본값: 3001)
NEST_PORT=3001

# CORS 설정
# 허용할 Origin들을 쉼표로 구분 (정규식 패턴도 지원: /pattern/)
# 예: http://localhost:3000,https://myapp.com,/.+\.mydomain\.com$/
NEST_CORS_ORIGINS=http://localhost:3000

# AWS DynamoDB 설정
# AWS 리전 (기본값: us-east-1)
AWS_REGION=us-east-1

# AWS 인증 정보
# AWS 액세스 키 ID (프로덕션 환경 필수)
AWS_ACCESS_KEY_ID=

# AWS 시크릿 액세스 키 (프로덕션 환경 필수)
AWS_SECRET_ACCESS_KEY=

# AWS 세션 토큰 (임시 인증 시 사용, 선택적)
AWS_SESSION_TOKEN=

# DynamoDB 설정
# 로컬 개발용 DynamoDB 엔드포인트 (선택적)
# DYNAMODB_ENDPOINT=http://localhost:8000

# DynamoDB 테이블 이름 접두사 (선택적)
# DYNAMO_TABLE_PREFIX=dev_

# Python 서비스 연동 설정
# TradingAgents Python API 서비스 URL (기본값: http://localhost:8000)
PYTHON_SERVICE_URL=http://localhost:8000

# 내부 API 인증
# 서버 간 통신용 인증 토큰 (필수)
INTERNAL_API_TOKEN=your-secret-internal-token

# 개발 환경 설정
# 토큰 인증을 스킵할지 여부 (개발 환경에서만 true로 설정, 기본값: false)
SKIP_TOKEN_AUTH=false
